# Project Rules

## Git Commit Style
- **JIRA Issue Keys**: Do NOT include JIRA issue keys (e.g. `EBLP-X`) in the git commit messages unless explicitly requested by the user. Use standard prefix style (e.g., `fix:`, `feat:`) otherwise.
